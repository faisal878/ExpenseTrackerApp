const intialStat = {
    users: [
        {}
    ]
}


const useReducer = (state = intialStat, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
export default useReducer;